{
  "id": "vision.subject_box",
  "version": 1,
  "purpose": "Locate the thumbnail's main subject (docs/DIRECTION.md sections 22-23): one box in frame fractions, so the composition pass can place the subject on a third and zoom to it. Low confidence is the honest answer for subjectless frames, and validation clamps or discards implausible boxes -- a wrong box recomposes the thumbnail around nothing.",
  "input_schema": {
    "type": "object",
    "required": [],
    "properties": {}
  },
  "output_schema": {
    "type": "object",
    "required": ["x", "y", "w", "h", "confidence"],
    "properties": {
      "x": {"type": "number", "minimum": 0, "maximum": 1},
      "y": {"type": "number", "minimum": 0, "maximum": 1},
      "w": {"type": "number", "minimum": 0, "maximum": 1},
      "h": {"type": "number", "minimum": 0, "maximum": 1},
      "confidence": {"type": "number", "minimum": 0, "maximum": 1}
    }
  }
}
